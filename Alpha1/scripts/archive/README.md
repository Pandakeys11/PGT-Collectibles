# Archived dev scripts

One-off catalog debugging utilities from earlier integration work. **Not wired to `package.json`** and not used in production builds.

| File | Notes |
|------|-------|
| `catalog-disambig-test.mjs` | Catalog disambiguation probe |
| `catalog-fraction-pick.mjs` | Collector fraction matching |
| `test-southern-islands-logic.mjs` | Southern Islands set logic |

For current probes, see [../README.md](../README.md) (dev-only section).
