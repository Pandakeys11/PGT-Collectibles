/** Top singles by FMV in set insight panels (highest → lowest). */
export const SET_INSIGHT_TOP_VALUE_LIMIT = 10;

/** Catalog candidates before Groq price refresh (wider pool, then slice to top 10). */
export const SET_INSIGHT_TOP_VALUE_POOL = 20;

/** Mover candidates ranked for the active set before UI splits up/down. */
export const SET_INSIGHT_MOVER_SEED_LIMIT = 24;

/** Rows per “trending up” / “trending down” column in compact binder insight. */
export const SET_MOVER_COLUMN_SIZE = 5;

/** Rows per column in full-width market intelligence rail. */
export const SET_MOVER_RAIL_COLUMN_SIZE = 8;
